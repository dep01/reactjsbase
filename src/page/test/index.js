import React from "react";
import { view } from "@risingstack/react-easy-state";

export default view(({}) => {
  return (
    <div
      style={{
        marginLeft: 5,
        flex: 1,
        minHeight: "100vh",
        backgroundColor: "blue",
      }}
    >
      <p>ini test</p>
    </div>
  );
});
